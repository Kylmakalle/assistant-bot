from core.stats import StatsEvents


class StatsEvents(StatsEvents):
    AMDBUILD = "amdbuild"
    AMDBUILD_ERROR = "amdbuild-error"
    REDDITBUILD = "redditbuild"
    REDDITBUILD_ERROR = "redditbuild-error"


# VIDEOCARDS = load_videocards()
# CPUS = load_cpus()
